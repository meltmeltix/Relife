class Destination {
    constructor(id, url, friendlyName, icon) {
        this.id = id
        this.url = url
        this.friendlyName = friendlyName
        this.icon = icon
    }
}

export default Destination